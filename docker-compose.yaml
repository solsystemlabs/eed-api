services:
  eed-api:
    image: ghcr.io/solsystemlabs/eed-api
    restart: always
    volumes:
      - ./src:/home/node/app/src
    expose:
      - '4000'
    ports:
      - '4000:3000'
    command: 'npm run start'
